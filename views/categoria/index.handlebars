<h2>Categorias:</h2>

<hr>

{{categorias}}

{{#each categorias}}
    <a href="/categorias/{{slug}}">
    <div class="card mt-4">
        <div class="card-body">
            <h4>{{nome}}</h4>
        </div>
    </div>
    </a>
{{else}}
    <h4 class="mt-3">Nenhuma categoria registrada.</h4>

{{/each}}
<div class="jumbotron">
  <h1 class="display-4">Bem-vindo ao meu Blog com Node </h1>
  <p class="lead">This is a simple hero unit, a simple jumbotron-style component for calling extra attention to featured content or information.</p>
  <hr class="my-4">
  <p>It uses utility classes for typography and spacing to space content out within the larger container.</p>
  <p class="lead">
    <a class="btn btn-primary btn-lg" href="#" role="button">Crei uma Conta</a>
  </p>
</div>
<hr>

<h2>Postagens Recentes:</h2>

{{#each postagens}}

    <div class="card mt-4">
        <div class="card-body">
            <h4>-- {{titulo}} --</h4>
            <p>{{descricao}}</p>
            <hr>
            <small>{{categoria.nome}} - {{data}}</small>
            <a href="/postagem/{{slug}}">
              <button class="btn btn-outline-primary float-right">Leia mais</button>
            </a>
        </div>
    </div>

{{else}}

    <h4 class="mt-3">Nenhuma postagem recente recente.</h4>

{{/each}}
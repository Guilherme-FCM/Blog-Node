<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        <small><strong>Data de publicação: </strong> {{postagem.data}}</small>
        <hr>
        <p class="text-justify">{{postagem.conteudo}}</p>
    </div>
</div>
